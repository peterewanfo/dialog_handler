import 'package:flutter/material.dart';

class ModalDialogWithBlurWidget extends StatelessWidget {
  const ModalDialogWithBlurWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
      height: 300,
      width: double.infinity,
    );
  }
}
